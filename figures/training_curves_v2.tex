% CIFAR-10 R-only 训练曲线 — CC-iGPT v2 (深窄 200ep, 历史实验)
% 编译: pdflatex training_curves_v2.tex   (或 latexmk -pdf)
% 嵌入论文: \input{figures/training_curves_v2.tex}
%
% 数据:
%   figures/training_curves_v2.csv  —— v2 (N=32/d=448, 200ep, EMA 0.9998, SWA from ep170, warmup 5ep)
%                                      198 epoch train_bpd (ep135-136 缺, pgfplots 自动跳过) + 99 val 点
%   landmark v2: ep1 train=12.07, ep10 val=3.34, ep90 val=2.879, ep170 SWA start, ep200 val=2.8322,
%                ep196 best val=2.8312 (in-training, 无 TTA/SWA/EMA/ensemble)
%   历史实验 v2（diagnostic protocol）：ensemble (best+SWA+EMA) + TTA hflip = 2.8296；旧 ±0.0854 口径已作废
%   正式单模型/no-TTA teacher-forced 重评：2.8328 bpd（10,000 张）

\documentclass[tikz,border=8pt]{standalone}
\usepackage{tikz}
\usepackage{pgfplots}
\usepackage{pgfplotstable}
\usepackage{amsmath,amssymb}
\pgfplotsset{compat=1.18}

\begin{document}
\begin{tikzpicture}[font=\footnotesize]
\begin{axis}[
    width=13cm, height=7cm,
    xlabel={\textbf{epoch}},
    ylabel={\textbf{bits/dim}},
    ylabel near ticks,
    xlabel near ticks,
    xmin=0, xmax=205,
    ymin=2.5, ymax=12,
    ytick={3,4,6,8,10,12},
    xtick={1,20,40,60,80,100,140,170,200},
    x tick label style={font=\scriptsize},
    axis line style={gray!55},
    tick style={gray!55},
    grid=major,
    grid style={dashed, gray!18, line width=0.3pt},
    unbounded coords=jump,                   % 关键：CSV 空字段当 NaN 跳过，曲线自动断开（v2 ep135-136 缺也由此处理）
    legend style={
        font=\scriptsize,
        draw=gray!35, fill=white,
        at={(0.97,0.97)}, anchor=north east,
        cells={anchor=west},
        row sep=1pt,
    },
    clip=false,
]

% --- SWA 启动竖虚线 (ep170) ---
\draw[densely dashed, gray!35!orange, line width=0.4pt]
    (axis cs:170, 2.5) -- (axis cs:170, 12);
\node[font=\scriptsize, orange!55!black, anchor=south west, rotate=90]
    at (axis cs:170, 2.5) {\,SWA start (last 31 ckpts)};

% --- v2 train_bpd 曲线 ---
\addplot+[
    mark=none, line width=0.8pt, color=blue!70!black,
] table[col sep=comma, x=epoch, y=train_bpd]
    {training_curves_v2.csv};
\addlegendentry{train bpd}

% --- v2 val_bpd 曲线 ---
\addplot+[
    mark=*, mark size=0.9pt, line width=0.8pt, color=orange!80!black,
] table[col sep=comma, x=epoch, y=val_bpd]
    {training_curves_v2.csv};
\addlegendentry{val bpd (every 2 ep)}

% --- ep196 / ep200 末段标注（右端对齐到图内右上空白区，竖向拉开避免重叠）---
\node[font=\scriptsize, orange!80!black, anchor=east] (annA)
    at (axis cs:200, 4.5)
    {ep196 best val \textbf{2.8312}\,$\to$\,ensemble$+$TTA $=$\,\textbf{2.8296}};
\node[font=\scriptsize, orange!80!black, anchor=east] (annB)
    at (axis cs:200, 3.8)
    {ep200 in-training: SWA \textbf{2.8276} / EMA \textbf{2.8301}};

% 引导虚线: 从标注指向 ep200 曲线末点 (~2.83)
\draw[densely dotted, orange!60!black, line width=0.3pt]
    (annA.south east) -- (axis cs:200, 2.83);
\draw[densely dotted, orange!60!black, line width=0.3pt]
    (annB.south east) -- (axis cs:200, 2.83);

\end{axis}

% --- 标题 ---
\node[font=\footnotesize\bfseries, anchor=south]
    at (current bounding box.north)
    {CC-iGPT v2 R-only training curve};

% --- 副标题（实验配置） ---
\node[font=\scriptsize, gray!50!black, anchor=north, yshift=-2pt]
    at (current bounding box.south)
    {\textit{N=32/d=448 (82.95M) $\cdot$ warmup 5ep $\cdot$ EMA 0.9998 $\cdot$ SWA last 31 ckpts (start ep 170) $\cdot$ formal single/no-TTA teacher-forced $=$ 2.8328 $\cdot$ historical ensemble+TTA $=$ 2.8296}};

\end{tikzpicture}
\end{document}
