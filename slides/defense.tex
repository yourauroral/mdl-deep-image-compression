% =============================================================
%  CC-iGPT v2 答辩 beamer (精简版，10 页，图片用户自插)
%  编译: xelatex defense.tex (双编译保 TOC/目录正确)
%  Overleaf: 上传本单文件，编译器选 XeLaTeX 即可
% =============================================================
\documentclass[10pt,aspectratio=169]{beamer}
\usetheme{metropolis}

\usepackage[UTF8]{ctex}      % Overleaf 自带，中英混排
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,fit,backgrounds}
\usepackage{graphicx}
\graphicspath{{../figures/}{./}}

% ── 配色（与 demo 一致）──
\definecolor{ours}{HTML}{6C8CFF}
\definecolor{accent}{HTML}{FF9800}
\definecolor{muted}{HTML}{8B8FA3}
\setbeamercolor{alerted text}{fg=accent}
\setbeamercolor{progress bar}{fg=ours}
\setbeamercolor{frametitle}{bg=ours}

% ── 占位框宏（用户后续用 \includegraphics 替换）──
% 用法: \figplaceholder{建议素材文件名}{宽}{高}
\newcommand{\figplaceholder}[3]{%
  \begin{center}
  \begin{tikzpicture}
    \draw[dashed, gray!55, line width=0.8pt]
      (0,0) rectangle (#2,#3);
    \node[align=center, gray!70] at (#2/2, #3/2)
      {\small\textit{[ 此处插入图片 ]}\\[2pt]
       \scriptsize\texttt{#1}};
  \end{tikzpicture}
  \end{center}
}

% ── 回填位宏（AutoDL 跑出下游数字后替换；全文搜 \fillme 即可定位）──
% 用法: \fillme{待填项说明}  → 渲染成醒目黄底「待填」标签
\newcommand{\fillme}[1]{\colorbox{yellow!75}{\textcolor{black}{\scriptsize\textbf{[待填]} #1}}}

% ── 标题信息（用户填空）──
\title{基于 MDL 原则的深度图像压缩系统的设计与实现}
\subtitle{CC-iGPT 双尺度条件式自回归压缩}
\author{[ 姓名 ]}
\institute{[ 学院 / 学校 ] \quad 指导教师: [ 导师姓名 ]}
\date{2026 年 5 月 28 日}

\begin{document}

% ============================== 1. 封面 ==============================
\maketitle

% ============================== 2. 命题 ==============================
\begin{frame}{命题：压缩即智能}
\textbf{Shannon (1948)} \;源编码定理：随机序列的最优编码长度下界为其负对数似然
\[
L^{\star}(x) \;=\; -\log_2 p(x) \quad \text{(bits)}.
\]

\textbf{MDL (Rissanen 1978)} \;给定模型族 $\mathcal{M}$，最优模型最小化「编码模型 + 编码数据」的总长度。
\[
\mathrm{MDL}(\theta, \mathcal{D}) \;=\; \underbrace{L(\theta)}_{\text{模型描述}} \;+\; \underbrace{L(\mathcal{D}\mid\theta)}_{\text{数据在模型下的描述}}
\]

\medskip
\textbf{对于图像 $x \in \{0,\dots,255\}^{H\times W\times C}$}，神经自回归把概率分解为
\[
p_\theta(x) \;=\; \prod_t p_\theta(x_t \mid x_{<t}),
\qquad
\text{\alert{bpd} } = \frac{\mathrm{CE}_\theta(x)}{\ln 2 \cdot H W C}.
\]

\medskip
\begin{itemize}
  \item \textbf{CE loss 直接等于压缩率} —— 训练目标与评估指标完全对齐
  \item 全程 \textbf{RGB-bit-exact} 域，不经色彩前端，与主流基线同域可比
\end{itemize}
\end{frame}

% ============================== 2b. 研究背景 ==============================
\begin{frame}{研究背景：神经无损压缩的两条血脉}
\begin{columns}[T]
\begin{column}{0.48\textwidth}
\textbf{为什么用神经 AR 做无损？}
\begin{itemize}\setlength{\itemsep}{3pt}
  \item 传统编解码器（PNG / WebP / FLIF）\\
        手工设计上下文 + 熵编码，建模能力受限
  \item 神经 AR：学习 $p_\theta(x)$ 逼近 Shannon 下界，\\
        \textbf{CE loss 直接 = 码长}
  \item 算术编码把 $p_\theta$ 转成真实 bitstream \\
        $\Rightarrow$ 严格无损、逐像素可还原
\end{itemize}
\end{column}
\begin{column}{0.50\textwidth}
\textbf{现有工作的两条血脉与缺口}
\begin{itemize}\setlength{\itemsep}{3pt}
  \item \textbf{密度血脉}：PixelCNN++ $\to$ PixelSNAIL \\
        $\to$ Image Transf. $\to$ Sparse Transf. (Child 2019) \\
        {\scriptsize 追 SOTA bpd，纯生成、\alert{无数字下游}}
  \item \textbf{表征血脉}：iGPT (Chen 2020) \\
        {\scriptsize AR 预训练做表征，\alert{不报 SOTA bpd}}
  \item \alert{\textbf{本工作}}：用 \textbf{MDL} 统一两者 \\
        {\scriptsize RGB-bit-exact 双尺度压缩（追 bpd）\\
         $+$ 下游主线（验证表征 / 生成 / 真实可解性）}
\end{itemize}
\end{column}
\end{columns}

\vfill
\centering\small
\color{muted}
回应导师反馈「下游不清晰」 $\Rightarrow$ \color{ours}\textbf{§5 MDL 下游主线：压得好 $\Rightarrow$ 学得好}
\end{frame}

% ============================== 2c. 国内外研究现状 ==============================
\begin{frame}{研究现状：国内外进展与缺口}
\begin{columns}[T]
\begin{column}{0.50\textwidth}
\textbf{国外}
\begin{itemize}\setlength{\itemsep}{2.5pt}\small
  \item \textbf{传统编解码器}：PNG / WebP / FLIF / JPEG-LS \\
        {\scriptsize 手工上下文 $+$ 熵编码，建模能力受限}
  \item \textbf{神经 AR 密度}：PixelRNN/CNN (van den Oord 2016) \\
        $\to$ PixelCNN++ $\to$ PixelSNAIL $\to$ Sparse Transf. \\
        {\scriptsize 逐代刷新 bpd，纯生成、\alert{无数字下游}}
  \item \textbf{Flow 无损}：L3C (Mentzer 2019)、IDF (Hoogeboom 2019) \\
        {\scriptsize 可逆映射 $+$ 精确似然，但建模容量受可逆约束}
  \item \textbf{AR 表征}：iGPT (Chen 2020) \\
        {\scriptsize 像素 AR 预训练做表征，\alert{放弃 SOTA bpd}}
\end{itemize}
\end{column}
\begin{column}{0.48\textwidth}
\textbf{国内}
\begin{itemize}\setlength{\itemsep}{2.5pt}\small
  \item \textbf{华为诺亚方舟}：iVPF (CVPR 2021)、\\
        iFlow (NeurIPS 2021) \\
        {\scriptsize 数值可逆 flow，解决 flow 无损的精度 $+$ 编码效率}
  \item \textbf{学习式压缩（以有损为主）}：中科大、\\
        南京大学等团队端到端图像压缩 \\
        {\scriptsize 优化 rate--distortion，\alert{非 bit-exact 无损}}
  \item {\scriptsize 国内像素级 \textbf{无损} AR 路线相对稀疏，\\
        多走 flow / 有损方向}
\end{itemize}

\medskip
\textbf{共同缺口}
\begin{itemize}\setlength{\itemsep}{2.5pt}\small
  \item 密度血脉追 bpd 但\alert{无下游数字}；表征血脉做下游但\alert{弃 bpd}
\end{itemize}
\end{column}
\end{columns}

\vfill
\centering\small\color{muted}
本工作定位 $\Rightarrow$ \color{ours}\textbf{RGB-bit-exact 像素 AR（追 bpd）$+$ MDL 下游主线（压得好 $\Rightarrow$ 学得好），统一两条血脉}
\end{frame}

% ============================== 3. 主表结果 ==============================
\begin{frame}{主表结果：CIFAR-10 RGB-bit-exact}
\begin{center}
\includegraphics[width=0.92\linewidth]{baseline.png}
\end{center}

\vspace{-2mm}
\centering
\small
\textbf{CC-iGPT v2 R-only}: \alert{\textbf{2.8296}} $\pm$ 0.0854 bpd \;
(82.95\,M params, 200 ep, ensemble + TTA hflip)

\smallskip
{\scriptsize vs PixelSNAIL 380M (2.85) \textbf{$-0.020$} \;\; $\cdot$ \;\; vs Sparse Transformer 59M (2.80) $+0.020$}
\end{frame}

% ============================== 3b. ImageNet64 第二轨道（in-training） ==============================
\begin{frame}{第二轨道：ImageNet 64$\times$64（训练中）}
\centering
\includegraphics[width=0.92\linewidth]{ImageNet64baseline.pdf}

\
\end{frame}

% ============================== 4. 模型架构总览（图） ==============================
\begin{frame}{模型架构：CC-iGPT 双尺度条件 AR}
\begin{center}
\includegraphics[height=0.78\textheight,keepaspectratio]{cc-igpt_arch.pdf}
\end{center}
\end{frame}

% ============================== 4b. 模型架构（文字） ==============================
\begin{frame}{模型架构：双尺度分解与联合训练}
\begin{columns}[T]
\begin{column}{0.48\textwidth}
\textbf{双尺度分解}
\begin{itemize}
  \item \textbf{Coarse}: DOWN $8{\times}8$ R-only \\
        $\Rightarrow$ 64 token, $\sim$2\% 预算
  \item \textbf{Fine}: $32{\times}32{\times}3$ sub-pixel AR \\
        $\Rightarrow$ 3072 token
\end{itemize}

\bigskip
\textbf{端到端联合训练}
\[
\mathcal{L} = \mathcal{L}_{\text{coarse}} + \mathcal{L}_{\text{fine}}
\]
无尺度间加权，回避多尺度 loss 平衡难题
\end{column}
\begin{column}{0.50\textwidth}
\textbf{联合 bits/dim}
\[
\mathrm{bpd}_{\text{total}} \;=\; \frac{\mathrm{CE}_c\,N_c + \mathrm{CE}_f\,N_f}{\ln 2 \cdot N_f}
\]

\medskip
其中 $N_c = 64$ (coarse 8$\times$8 R), $N_f = 3072$ ($32{\times}32{\times}3$)。

\medskip
\textbf{实测分解（CIFAR-10 主表）}
\begin{itemize}
  \item $\mathrm{CE}_c = 4.3302$ \quad bpd 占比 \alert{4.6\%}
  \item $\mathrm{CE}_f = 1.8711$ \quad bpd 占比 \alert{95.4\%}
  \item $\Rightarrow$ \alert{\textbf{2.8296}} bpd
\end{itemize}
\end{column}
\end{columns}
\end{frame}

% ============================== 5. Coarse pipeline (图) ==============================
\begin{frame}{Coarse GPT pipeline —— $\sim$2\% 预算的「轮廓码」}
\begin{center}
\includegraphics[height=0.78\textheight,keepaspectratio]{Coarse branch pipeline.pdf}
\end{center}
\end{frame}

% ============================== 5b. Coarse pipeline (文字) ==============================
\begin{frame}{Coarse 分支：模块组成与产出}
\begin{columns}[T]
\begin{column}{0.48\textwidth}
\textbf{下采样}
\begin{itemize}
  \item \texttt{F.adaptive\_avg\_pool2d(x, 8)} \\
        float 域 $32 \to 8$
  \item 取 R 通道 $\Rightarrow$ $8{\times}8{\times}1 = 64$ token
\end{itemize}

\bigskip
\textbf{Coarse iGPT（浅层）}
\begin{itemize}
  \item $d_{\text{model}} = 256$, $N = 6$ 层
  \item 标准 NTP 训练
  \item Weight Tying, RoPE, QK-Norm
\end{itemize}
\end{column}
\begin{column}{0.50\textwidth}
\textbf{产出与定位}
\begin{itemize}
  \item $\mathrm{CE}_c = \alert{4.3302}$ \\
        $\Rightarrow$ 直接进 bitstream
  \item bpd 占比仅 \alert{4.6\%}（$\sim$2\% 预算）
  \item 量化 token 作为 \textbf{ctx 桥梁} \\
        交给 fine 分支条件化
\end{itemize}

\bigskip
\textbf{设计动机}
\begin{itemize}
  \item 用极少 bit 编码全局轮廓
  \item 让 fine 端聚焦高频细节
  \item 类比 Laplacian Pyramid (1983)
\end{itemize}
\end{column}
\end{columns}
\end{frame}

% ============================== 6. Fine pipeline (图) ==============================
\begin{frame}{Fine GPT pipeline —— 主压缩主体（占 95.4\% bpd）}
\begin{center}
\includegraphics[height=0.78\textheight,keepaspectratio]{Fine branch pipeline.pdf}
\end{center}
\end{frame}

% ============================== 6b. Fine pipeline (文字) ==============================
\begin{frame}{Fine 分支：Ctx 注入与子像素 AR}
\begin{columns}[T]
\begin{column}{0.48\textwidth}
\textbf{Ctx 注入}
\begin{itemize}
  \item coarse 量化 token \\
        $\to$ bit-exact ctx
  \item $\alpha \cdot \mathrm{ctx}$ \emph{additive} 入口
  \item $\alpha = \alert{0.1009}$（实测）\\
        \scriptsize 模型自适应注入强度
\end{itemize}

\bigskip
\textbf{Fine iGPT（深窄）}
\begin{itemize}
  \item $d_{\text{model}} = 448$, $N = 32$ 层 \\
        (\textbf{82.95\,M} params)
  \item Flash Attn v2, SwiGLU, \\
        RoPE, RMSNorm (post-norm)
\end{itemize}
\end{column}
\begin{column}{0.50\textwidth}
\textbf{子像素 AR 序列}
\[
[R_0,\, G_0,\, B_0,\; R_1,\, G_1,\, B_1,\; \dots]
\]
Causal mask 自然实现
\[
p(G \mid R), \quad p(B \mid R, G)
\]
通道间条件依赖无需额外参数。

\bigskip
\textbf{产出}
\begin{itemize}
  \item $\mathrm{CE}_f = \alert{1.8711}$
  \item bpd 占比 \alert{95.4\%}（主压缩主体）
\end{itemize}
\end{column}
\end{columns}
\end{frame}

% ============================== 7. Bit-exact ctx (图) ==============================
\begin{frame}{Bit-exact ctx pipeline}
\begin{center}
\includegraphics[height=0.78\textheight,keepaspectratio]{bit-exact ctx pipeline.pdf}
\end{center}
\end{frame}

% ============================== 7b. Bit-exact ctx (文字) ==============================
\begin{frame}{Bit-exact ctx pipeline：可解码的条件注入}
\textbf{Pipeline}
\begin{center}
\small
\texttt{coarse 量化 token} $\to$ \texttt{/255 反量化} $\to$ \texttt{bilinear UP $32{\times}32$}
$\to$ \texttt{re-tokenize} $\to$ \texttt{fine.token\_embed} $\to$ \texttt{AR shift [:, 1:]} $\to$ $\alpha\cdot$\texttt{ctx}
\end{center}

\medskip
\begin{itemize}
  \item \textbf{Encoder / decoder 共用同一函数} —— bitstream 真实可解，不是「训练时 oracle」
  \item \textbf{整条 ctx 通路仅引入 1 个标量参数 $\alpha$} —— 零新参注入，避开多尺度联合 AR 的 loss 平衡难题
  \item 复用 \texttt{fine.token\_embed} 完成 ctx 编码 —— 与下游 token 共享语义空间
  \item AR shift \texttt{[:, 1:]} —— ctx[i] 对应 fine 被预测位置 $i$（PixelCNN++ conditional 标准语义）
\end{itemize}
\end{frame}

% ============================== 7c. 编码/解码数据流程图 ==============================
\begin{frame}{Codec 数据流：编码 / 解码对称性}
\begin{center}
\includegraphics[height=0.78\textheight,keepaspectratio]{CC-iGPT codec dataflow.pdf}
\end{center}
\end{frame}

% ============================== 7d. Codec 文字说明 ==============================
\begin{frame}{Codec 数据流：encoder / decoder 同管线}
\begin{columns}[T]
\begin{column}{0.48\textwidth}
\textbf{Encoder（压缩端）}
\begin{enumerate}
  \item DOWN: $x \to x_c$ (R 8$\times$8)
  \item Coarse iGPT $\to$ 概率 $p_c$ \\
        $\Rightarrow$ 算术编码进 bitstream
  \item $x_c$ $\to$ \textbf{ctx pipeline}
  \item Fine iGPT(\,$x_f$ \,$|$ $\alpha\cdot$ctx) \\
        $\Rightarrow$ 概率 $p_f$ \\
        $\Rightarrow$ 算术编码进 bitstream
\end{enumerate}
\end{column}
\begin{column}{0.50\textwidth}
\textbf{Decoder（解压端）}
\begin{enumerate}
  \item 算术解码 coarse token $\hat{x}_c$
  \item $\hat{x}_c$ $\to$ \textbf{同一 ctx pipeline} \\
        {\scriptsize（bit-exact，与 encoder 完全一致）}
  \item Fine iGPT(\,$\cdot$ $|$ $\alpha\cdot$ctx) \\
        逐 token AR 解码 $\hat{x}_f$
  \item 还原 RGB $\hat{x} = x$
\end{enumerate}

\bigskip
\centering
\alert{\textbf{无损 \quad bit-exact \quad 真实可解}}
\end{column}
\end{columns}
\end{frame}

% ============================== 8. 训练 (图) ==============================
\begin{frame}{训练动力学：CIFAR-10 与 ImageNet64 训练曲线}
\centering
% 两图按同一高度并排（CIFAR 曲线偏扁，IN64 偏方）；resizebox 锁定总宽=\linewidth，等高且不出框
\resizebox{\linewidth}{!}{%
  \includegraphics[height=4cm]{CC-iGPT R-only training curves.pdf}\hspace{3mm}%
  \includegraphics[height=4cm]{ImageNet_trainning_curve.pdf}%
}

\vspace{2mm}
{\centering\footnotesize\color{muted}
左：CIFAR-10 v2 R-only 200 ep（主表 2.8296）\quad 右：ImageNet 64$\times$64 12 ep cosine（主表 ensemble+TTA 3.4800）\par}
\end{frame}

% ============================== 9. Triton Kernel (图) ==============================
\begin{frame}{工程：8 个手写 Triton Kernel —— Speedup}
\begin{center}
\includegraphics[height=0.78\textheight,keepaspectratio]{Triton-kernel-speedup-bar-chart.pdf}
\end{center}
\end{frame}

% ============================== 9a. Triton Kernel 显存 (图) ==============================
\begin{frame}{工程：Triton Kernel —— 显存节省}
\begin{center}
\includegraphics[height=0.78\textheight,keepaspectratio]{Triton-kernel-memory-savings-bar-chart.pdf}
\end{center}
\end{frame}

% ============================== 9b. Triton Kernel (文字) ==============================
\begin{frame}{工程：Kernel 清单与反面案例}
\begin{columns}[T]
\begin{column}{0.50\textwidth}
\textbf{7 个进入训练栈}
\begin{itemize}\setlength{\itemsep}{2pt}
  \item Flash Attention v2 ($\sim$960 行)
  \item Fused CE + z-loss
  \item Fused RMSNorm
  \item Fused Add + RMSNorm
  \item Fused SwiGLU
  \item Fused RoPE
  \item Fused Attn + RoPE
\end{itemize}
\end{column}
\begin{column}{0.48\textwidth}
\textbf{1 个反面案例}
\begin{itemize}
  \item \texttt{fused\_linear\_ce}
  \item $V = 256$ 下 roofline 分析 \\
        $\Rightarrow$ compute-bound \\
        $\Rightarrow$ 失去 cuBLAS GEMM 利用率
  \item 实测比 PyTorch 基线慢 \\
        \alert{保留作工程严谨性证据}
\end{itemize}

\bigskip
\textbf{决策原则}
\begin{itemize}
  \item 不是「写了就保留」
  \item Roofline + 实测双验证
  \item 负收益的 kernel 也是结论
\end{itemize}
\end{column}
\end{columns}
\end{frame}

% ============================== 10. 下游任务：MDL 主线总览 ==============================
\begin{frame}{下游任务：MDL 主线「压得好 $\Rightarrow$ 学得好」}
同一份 CC-iGPT 权重（\textbf{不重训、不微调}），从三个侧面验证它学到的是\\
自然图像的通用统计，而非数据集记忆：

\medskip
\centering\small
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{侧面} & \textbf{任务} & \textbf{验证的 MDL 命题} \\
\midrule
表征 & Linear probe       & 线性可分表征更强        \\
生成 & 图像补全 (inpaint) & AR 条件生成能力         \\
\midrule
\multicolumn{3}{@{}l}{\textbf{地基}\;：真实可解性 —— 图像 $\leftrightarrow$ .bin 逐 token gold 编解码 bit-identical，} \\
\multicolumn{3}{@{}l}{\quad\quad\;\; 证明所报 bpd 是\alert{真实可逆码长}而非 proxy} \\
\bottomrule
\end{tabular}

\medskip
\footnotesize\color{muted}
representation 血脉承 iGPT，density 血脉承 Sparse Transformer（纯生成）；\\
本工作用 MDL 把两者串成一条因果链。
\end{frame}

% ============================== 下游 1 表征：Linear probe (图) ==============================
\begin{frame}{下游 1 表征：Linear probe（逐层 top-1 准确率）}
\begin{center}
\includegraphics[width=\linewidth,keepaspectratio]{下游任务线性表征.png}
\end{center}
\end{frame}

% ============================== 下游 1 表征：Linear probe (文字) ==============================
\begin{frame}{下游 1 表征：probe 方法与结果}
\begin{columns}[T]
\begin{column}{0.50\textwidth}
\small
\textbf{方法}
\begin{itemize}\setlength{\itemsep}{2pt}
  \item 冻结预训练 CC-iGPT，逐层取 hidden state
  \item 各层训一个线性分类器，测 CIFAR-10 top-1
  \item v2 fine $+\,\alpha\cdot$ctx 注入，32 层全扫
\end{itemize}

\bigskip
\textbf{MDL 命题}
\begin{itemize}
  \item 压得越好 $\Rightarrow$ 线性可分表征越强
  \item ctx 注入显著抬高中层语义表征
\end{itemize}
\end{column}
\begin{column}{0.50\textwidth}
\small
\textbf{结果}
\begin{itemize}\setlength{\itemsep}{2pt}
  \item best \textbf{L19} = \alert{\textbf{79.33\%}}
  \item iGPT-S baseline best L22 = 66.93\%
  \item \alert{\textbf{+12.4\,pp}}
\end{itemize}

\bigskip
{\scriptsize L0$\to$L19 单调上升至峰值，末段缓降 —— AR 模型典型表征曲线（末层过拟合到 token-level NLL，丢弃部分语义）。}
\end{column}
\end{columns}
\end{frame}

% ============================== 下游 2 生成：图像补全 (图) ==============================
\begin{frame}{下游 2 生成：图像补全（AR inpainting）}
% 三联图：原图 | 已知上半(灰=待补) | AR 补全
\begin{center}
\includegraphics[width=0.95\linewidth,height=0.78\textheight,keepaspectratio]{下游任务图像补全.png}
\end{center}
\end{frame}

% ============================== 下游 2 生成：图像补全 (文字) ==============================
\begin{frame}{下游 2 生成：补全方法与命题}
\begin{columns}[T]
\begin{column}{0.50\textwidth}
\small
\textbf{方法}
\begin{itemize}\setlength{\itemsep}{2pt}
  \item 给前 keep\_frac 比例的 raster token（$\approx$上半）
  \item AR 续采样补全下半（temperature $+$ top-$k$）
  \item 无 KV-cache，逐 token 完整 forward
\end{itemize}

\bigskip
\textbf{MDL 命题}
\begin{itemize}
  \item AR 条件生成能力（Sparse Transformer 同款下游）
  \item 模型天然支持，不重训
\end{itemize}
\end{column}
\begin{column}{0.50\textwidth}
\small
\textbf{产出}
\begin{itemize}\setlength{\itemsep}{2pt}
  \item 每行：原图 $|$ 已知上半(灰=待补) $|$ AR 补全
  \item 前端 demo 实时交互版（可调 keep\% / 温度）
\end{itemize}

\bigskip
\textbf{结果}
\begin{itemize}\setlength{\itemsep}{2pt}
  \item 仅 CIFAR v2 实用（fine 3072 token）
  \item 前端实时补全面板（见左图），可现场换图 / 调 keep\% / 温度
\end{itemize}
\end{column}
\end{columns}
\end{frame}

% ============================== 下游 3 地基：真实可解性 (图) ==============================
\begin{frame}{下游 3 地基：真实可解性 —— 图像 $\leftrightarrow$ .bin 编解码}
\begin{center}
\includegraphics[width=0.95\linewidth,height=0.78\textheight,keepaspectratio]{下游任务真实编码解码.png}
\end{center}
\end{frame}

% ============================== 下游 3 地基：真实可解性 roundtrip (文字) ==============================
\begin{frame}{下游 3 地基：可解性方法与命题}
\begin{columns}[T]
\begin{column}{0.50\textwidth}
\small
\textbf{方法}
\begin{itemize}\setlength{\itemsep}{2pt}
  \item 手写 32-bit WNC 算术编解码
  \item 图像 $\to$ token $\to$ 逐步条件分布 \\
        $\to$ 真实 bitstream $\to$ 盲解码还原
  \item 逐像素断言 bit-identical（前端可现场上传图 $\leftrightarrow$ 下载 .bin）
\end{itemize}

\bigskip
\textbf{可逆性保证}
\begin{itemize}
  \item 无 KV-cache，\textbf{encode / decode 同一逐 token forward}
  \item $\Rightarrow$ logits 逐位相同 $\Rightarrow$ 严格可逆（bit-exact）
\end{itemize}
\end{column}
\begin{column}{0.50\textwidth}
\small
\textbf{MDL 命题（整条主线的地基）}
\begin{itemize}\setlength{\itemsep}{2pt}
  \item 所报 bpd 是\alert{真实可逆码长}，非 proxy
  \item 区别于只报 bpd、不验可逆性的工作
\end{itemize}

\bigskip
\textbf{结果}
\begin{itemize}\setlength{\itemsep}{2pt}
  \item codec 单测 6/6 过 $+$ \textbf{2/2 图 bit-identical}
  \item achieved 码长 $\approx$ 理论 NLL，\alert{量化+收尾 overhead 仅 0.1–0.2\%}
  \item {\scriptsize 例：img0 3.093 bpd (NLL 3.090) / img1 2.322 (NLL 2.316)，逐图随复杂度变，关键是 overhead 极小 $\Rightarrow$ 报的 bpd = 真码长}
\end{itemize}
\end{column}
\end{columns}
\end{frame}

% ============================== 11. 总结 ==============================
\begin{frame}{总结：三大创新 $+$ MDL 主线}
\begin{columns}[T]
\begin{column}{0.50\textwidth}
\small
\textbf{三大创新}

\smallskip
\textbf{1 方法} —— 零新参的双尺度条件注入 \\
{\scriptsize coarse 量化 token $\to$ bit-exact ctx $\to$ $\alpha\cdot$additive，仅 1 个标量参数}

\smallskip
\textbf{2 工程} —— 8 个手写 Triton kernel \\
{\scriptsize 7 进入训练栈 $+$ 1 反面案例（$V{=}256$ roofline 证伪）}

\smallskip
\textbf{3 分析} —— RGB-bit-exact 主表（双轨道） \\
{\scriptsize CIFAR-10 \alert{\textbf{2.8296}} 超越 PixelSNAIL 380M (2.85)、逼近 Sparse Transformer 59M (2.80)，82.95M 参数；\\
ImageNet 64$\times$64 第二轨道主表 ensemble+TTA \textbf{3.4800}（超过 SPN 3.52，逼近 Sparse Transformer 152M 3.44）}
\end{column}
\begin{column}{0.50\textwidth}
\small
\textbf{MDL 主线：压得好 $\Rightarrow$ 学得好}

\smallskip
\begin{itemize}\setlength{\itemsep}{2pt}
  \item \textbf{表征} —— probe best \alert{79.33\%}（$+$12.4\,pp）
  \item \textbf{生成} —— AR 图像补全
  \item \textbf{地基} —— 真实可解性（图像 $\leftrightarrow$ .bin bit-identical）
\end{itemize}

\smallskip
{\scriptsize 把压缩与表征 / 生成 / 可解性串成因果链，统一 density 与 representation 两条血脉。}
\end{column}
\end{columns}

\vfill
\centering
\Large \color{ours} 感谢聆听，敬请批评指正
\end{frame}

\end{document}
